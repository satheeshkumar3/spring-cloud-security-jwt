server:
  port: 9296

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/satheeshkumar3/spring-cloud-security-config-server.git
          clone-on-start: true
          default-label: main
